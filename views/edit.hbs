<h2 class="m-4">Edit Post</h2>

<form action="/posts/{{post.id}}?_method=PUT" method="POST">
	<div class="form-control">
		<label for="title" class="form-label">Título</label>
		<input name="title" type="text" class="form-control" id="title" value="{{post.title}}" placeholder="Ingrese el nombre del post">
	</div>

	<div class="form-control">
		<label for="body" class="form-label">Cuerpo</label>
		<textarea name="body" class="form-control" id="body" rows="3">{{post.body}}</textarea>
	</div>

	<button class="btn btn-primary mt-4">Enviar</button>
</form>